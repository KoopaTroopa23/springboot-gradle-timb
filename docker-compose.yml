version: '3.8'

services:
  app:
    build: .
    container_name: springboot-gradle-timb
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
